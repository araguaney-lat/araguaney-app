# Files whose comments are still in Spanish — Phase 32.
#
# It is empty, and that is the end state: no comment under lib/ or test/ is in
# Spanish any more. The list stays because the test that reads it stays. From
# here it can only be wrong in one direction — a new file with a Spanish
# comment fails the check instead of being added here, which is what the list
# was for while there were 255 of them.
#
# The rule the check applies: quotations and code in backticks do not count.
# They are evidence, not prose.
